import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>My Little Pony Clicky Game!</h1>
        <p>Click on a pony to earn points, but don't click one more than once!</p>
    </div>
);

export default Title;

